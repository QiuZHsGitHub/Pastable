# Pastable 模块功能文档总览

本文档集按当前代码结构将项目拆分为 9 个模块，并为每个模块提供详细说明。

## 模块清单

| 编号 | 模块名称 | 主要职责 | 详细文档 |
| --- | --- | --- | --- |
| 01 | 应用入口与生命周期模块 | 应用启动、状态栏入口、全局快捷键、浮层窗口开关 | [01-应用入口与生命周期模块.md](./01-应用入口与生命周期模块.md) |
| 02 | 剪贴板采集与粘贴模块 | 轮询系统剪贴板、内容快照、去抖、回写与模拟粘贴 | [02-剪贴板采集与粘贴模块.md](./02-剪贴板采集与粘贴模块.md) |
| 03 | 数据模型与内容解析模块 | 剪贴板记录数据结构、内容派生属性、类型识别、OCR/颜色解析 | [03-数据模型与内容解析模块.md](./03-数据模型与内容解析模块.md) |
| 04 | 存储与去重持久化模块 | SwiftData 容器、新增/更新去重、历史清理、数据库体积信息 | [04-存储与去重持久化模块.md](./04-存储与去重持久化模块.md) |
| 05 | 状态管理与搜索筛选模块 | 全局 UI 状态、节流搜索、分类过滤、展示列表聚合 | [05-状态管理与搜索筛选模块.md](./05-状态管理与搜索筛选模块.md) |
| 06 | 主界面与交互编排模块 | 顶部筛选/搜索、卡片列表事件转发、快捷键行为、菜单操作 | [06-主界面与交互编排模块.md](./06-主界面与交互编排模块.md) |
| 07 | 卡片渲染与媒体缓存模块 | 卡片视觉渲染、图标/缩略图缓存、RTF 背景提取、自适应色 | [07-卡片渲染与媒体缓存模块.md](./07-卡片渲染与媒体缓存模块.md) |
| 08 | 设置中心与偏好项模块 | 通用设置、存储设置、快捷键设置、关于页 | [08-设置中心与偏好项模块.md](./08-设置中心与偏好项模块.md) |
| 09 | 主题国际化与基础扩展模块 | 主题色板、本地化文案、通用扩展工具与类型常量 | [09-主题国际化与基础扩展模块.md](./09-主题国际化与基础扩展模块.md) |

## 代码层模块关系

1. `PastableApp` 启动后将控制权交给 `AppDelegate`。
2. `AppDelegate` 启动 `Clipboard` 监听并创建 `FloatingPanel` + `ContentView`。
3. `Clipboard` 采集到快照后创建 `HistoryItem` 并交给 `Storage` 去重入库。
4. `ContentView` 通过 `@Query` 读取数据并同步到 `AppState`。
5. `AppState` 统一执行搜索与分类筛选，输出 `displayItems`。
6. UI 组件渲染 `displayItems`，用户点击后通过 `Clipboard` 完成复制与粘贴。
7. 设置页通过 `@AppStorage` 写入偏好，核心模块按 key 读取行为配置。

## 文档适用范围

- 以当前仓库代码为准（`/Pastable` 目录下 Swift 源码）。
- 文档重点描述“当前已实现功能”，并在每个模块中标注已知约束与未接入项。
